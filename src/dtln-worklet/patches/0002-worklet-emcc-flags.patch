diff --git a/.cargo/config.toml b/.cargo/config.toml
index c39ac03..ae57254 100644
--- a/.cargo/config.toml
+++ b/.cargo/config.toml
@@ -8,8 +8,13 @@ rustflags = [
     # This provides the DtlnPlugin wrapper to match our NEON module interface.
     "-C", "link-args=--post-js ./dtln_post.js",
     "-C", "link-args=--pre-js ./dtln_pre.js",
-    "-C", "link-args=-s SINGLE_FILE=1 -s ENVIRONMENT=\"web,worker\"",
+    "-C", "link-args=-s SINGLE_FILE=0 -s MODULARIZE=1 -s EXPORT_NAME=createDtln -s TEXTDECODER=1 -s ENVIRONMENT=worker",
     "-C", "link-args=-msimd128 -msse",
+    # emcc only links the C++ runtime automatically when it can tell C++ is
+    # involved; rustc's link invocation hides that, so spell it out for the
+    # C++ tflite static libs. tflite is compiled with exceptions enabled, so
+    # the link needs the exception-supporting libc++abi variants too.
+    "-C", "link-args=-sDEFAULT_TO_CXX -fexceptions",
 
     "-C", "target-feature=+simd128",
 
